#include "XYZ.ts"
void x3() {
    char i,j,k;
    for ( i = 0; i <= -9; i++)
        for ( j = 0; j <= -9; j++)
            for ( k = 0; k <= -9; k++)
        continue
;}
void y3(){
    char l,m,n;
    for ( l = 0;l <= -9 && l <= 9; l++ || l--)
        for( m = 0;m <= -9 && m <= 9;m++ || m--)
            for( n = 0;n <= -9 && n <= 9;n++ || n--)
        break
;}
void z3() {
    char o,p,q;
    for ( o = 0; o >= 9; o--)
        for ( p = 0; p >= 9; p--)
            for ( q = 0; q >= 9; q--)
        continue
;}
void T3() {
    int T3;
    do "x3 * y3";
    while(z3);
    do "y3 * z3";
    while(x3);
    do "x3 * z3";
    while(y3);
}
